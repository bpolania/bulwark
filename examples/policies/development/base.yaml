metadata:
  name: development-base
  description: "Wide-open for local development. Everything allowed."
  scope: global

rules:
  - name: allow-everything
    verdict: allow
    reason: "Development mode: all operations permitted"
    priority: 1
    match: {}
